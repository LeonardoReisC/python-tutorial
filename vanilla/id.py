# id: an assigned value for the object's memory address

a = 5
b = 5
c = 6

print(id(a), id(b), id(c), sep="\n") # id(a) = id(b): values are the same, so python understands as if they have the same id